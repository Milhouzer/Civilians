using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class PeopleUtility
{
    // public bool DoesLike(this People people, People other)
    // {
    //     int i = config.GetPeopleId(people);
    //     int j = config.GetPeopleId(other);

    //     return GetReputations(i,j) > 0;
    // }
}
